# HealthGame

Course project for distributed information systems.

Structure:
- `backend` - Spring Boot REST API
- `frontend` - React SPA
- `infra` - Docker Compose for local demo

Current state:
- backend skeleton with Flyway, PostgreSQL, security baseline and auth endpoints
- frontend skeleton with Vite + React
- initial database migration based on provided schema and approved extensions
